{
  "name": "makkalurimai-publications",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "astro-app",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=astro-app",
    "build": "npm run build --workspace=astro-app",
    "preview": "npm run preview --workspace=astro-app",
    "seed": "tsx scripts/seed.ts",
    "db:generate": "drizzle-kit generate",
    "db:push": "drizzle-kit push"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31.9",
    "tsx": "^4.21.0",
    "typescript": "^5"
  }
}